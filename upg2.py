# 2. Write a Python function to convert an Integer to a string in any base

data = 8


def convert_int_to_str(self):
    """Below is the easy way to convert integer to string, as in Python we can simply use str to define the type.
       No need to print the result in the objective, but I included it for clarification, as the different results.
    """
    print(str(self))
    print(type(self))
    print(type(str(self)))


convert_int_to_str(data)
